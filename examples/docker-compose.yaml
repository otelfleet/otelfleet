services:
  supervisor:
    network_mode: host
    image: ghcr.io/otelfleet/opamp-supervisor:0.156.0
    command: ["--config", "/etc/otel/supervisor.yaml"]
    volumes:
      - ./supervisor.yaml:/etc/otel/supervisor.yaml:ro
      - ./otel/config.yaml:/etc/otel/config.yaml:ro
      # Persist supervisor state (storage.directory in supervisor.yaml).
      - supervisor-data:/etc/otel/supervisor-data

volumes:
  supervisor-data:
